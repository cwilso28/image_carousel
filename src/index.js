import "./styles.css"

let ordered_list = document.getElementById("carousel-container");
let line_items = ordered_list.getElementsByTagName("li");
console.log(line_items.length);

// Image carousel status

// Adjust highlighted button as image carousel rotates or is rotated

// 